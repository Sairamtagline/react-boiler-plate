import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#6ec0f3',
      light: '#1de2e1',
    },
    secondary: {
      main: '#d4aa71',
    },
    white: {
      main: '#ffffff',
    },
  },
})
export default theme
